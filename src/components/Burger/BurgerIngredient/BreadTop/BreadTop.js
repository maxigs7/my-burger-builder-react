import React from 'react';

import classes from './BreadTop.css';

const breadTop = (props) => (
  <div className={classes.BreadTop}>
    <div className={classes.Seeds1}></div>
    <div className={classes.Seeds2}></div>
  </div>
);

export default breadTop;
